{
  "name": "blgg-platform",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:ipselon/blgg-platform.git",
    "directory": "."
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0",
  "workspaces": [
    "platform/*"
  ],
  "scripts": {
    "build-platform": "turbo run build",
    "deploy-platform": "turbo run build && turbo run deploy",
    "destroy-platform": "turbo run destroy",
    "admin-pwa": "turbo run dev --filter=admin-pwa",
    "admin-pwa-server": "turbo run preview --filter=admin-pwa",
    "admin-api-server": "turbo run preview --filter=admin-api",
    "web-app": "turbo run dev --filter=web-app",
    "web-app-server": "turbo run start --filter=web-app"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "turbo": "^1.10.16"
  }
}
